meta {
  name: GET /projects/:id
  type: http
  seq: 6
}

get {
  url: http://localhost:3000/api/projects
  body: none
  auth: inherit
}

body:json {
  {
    "name": "{{$randomWord}}",
    "userId": "{{$randomUUID}}",
    "boards": []
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
