meta {
  name: POST /projects
  type: http
  seq: 1
}

post {
  url: http://localhost:3000/api/projects
  body: json
  auth: inherit
}

body:json {
  {
    "name": "{{$randomWord}}",
    "userId": "{{$randomUUID}}",
    "boards": []
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
